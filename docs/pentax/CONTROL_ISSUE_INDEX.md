# Pentax/Ricoh generic-control issue index

Umbrella: #52
Discovery: #51

Current child issues:
- #53 `aperture`
- #54 `imageformat`
- #55 `imagequality`

Additional children are created for `autofocus`, `autofocusdrive`, `manualfocus`, `manualfocusdrive`, and `capturetarget` so each semantic control can be implemented/tested/closed independently.

A child closes only when its own model gates, tests and hardware acceptance criteria are met. Closing one control never implies the rest of #52 is complete.
